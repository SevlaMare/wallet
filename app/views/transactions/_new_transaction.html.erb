<%= form_with(model: @transaction) do |form| %>
  <%= form.label(:name, 'Matter: ') %>
  <%= form.text_field(:name) %>

  <%= form.label(:amount, 'Amount: ') %>
  <%= form.number_field(:amount) %>

  <%= form.submit('Register it!') %>
<% end %>
